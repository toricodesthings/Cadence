# Shared Packages

Shared workspace packages should live here once logic needs to be reused across multiple apps.

Examples:

- shared UI primitives
- API clients or generated SDKs
- validation and domain model packages
- TypeScript, ESLint, or other reusable tooling presets
