# Cadence Desktop

This directory is reserved for a future Tauri-based desktop app.

When desktop work begins, it should be added here as another workspace app and consume shared packages from `packages/` rather than duplicating logic from the web or mobile clients.
