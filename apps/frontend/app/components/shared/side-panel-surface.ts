/** Background shared by every right-rail panel (editors, Overview, assistant); matches the page header. Add `photo-shell-surface` where the panel sits over a photo background. */
export const SIDE_PANEL_SURFACE = "surface-shell";
