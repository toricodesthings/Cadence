// The app's `UserSettings` is the full view the API returns (`SettingsView`).
export { SETTINGS_DEFAULTS } from "@cadence/contracts/settings";
export type { DeepPartial, PersonalEvent, SettingsView as UserSettings } from "@cadence/contracts/settings";
